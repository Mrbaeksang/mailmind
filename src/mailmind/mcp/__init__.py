"""Configuration for the official MCP servers the agent integrates.

Gmail MCP (Google, remote) and MongoDB MCP (stdio). This package only holds
config/policy — the live connection is wired in the agent layer.
"""
